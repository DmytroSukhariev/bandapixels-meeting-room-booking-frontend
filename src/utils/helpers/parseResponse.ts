import { IBookedInfo } from '../../types/IBookedInfo';
import { IBookedRoom } from '../../types/IBookedRoom';

export const parseResponse = (bookedRoomInfo: IBookedRoom) => {
  const res: IBookedInfo[] = [];

  //   issuerFirstName: string;
  //   issuerLastName: string;
  //   meetingRoomId: number;
  //   description?: string;
  //   numberOfPeople?: number;
  //   start: Date;
  //   end: Date;

  return res;
};
